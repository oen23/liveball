services:
  postgres:
    image: postgres:15-alpine
    container_name: postgres_db
    restart: always
    environment:
      POSTGRES_USER: myuser
      POSTGRES_PASSWORD: mypassword
      POSTGRES_DB: mydatabase
    ports:
      - "5433:5432"  # Используем другой порт на хосте
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: